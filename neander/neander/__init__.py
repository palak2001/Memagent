"""Neander — a memory-persistent conversational agent.

Provides cross-session recall via atomic fact extraction, semantic retrieval,
and conflict resolution, all without adding to first-token latency.
"""

__version__ = "0.1.0"
